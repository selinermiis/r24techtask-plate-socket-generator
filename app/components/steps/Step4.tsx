'use client';

import React from 'react';

interface Step4Props {
  onComplete?: () => void;
}

export default function Step4({ onComplete }: Step4Props) {
  return (
    <div>
      <h1>Step 4</h1>
    </div>
  );
}
